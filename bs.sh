#!/bin/bash
# A sample Bash script, type .\bs.sh in terminal to run script
# or type $bash myscript.sh 
# Dont' fucking change the first line of bash code. It's called SheBang or something like that
browser-sync --server --files "*.html, css/*.css, js/*.js"
